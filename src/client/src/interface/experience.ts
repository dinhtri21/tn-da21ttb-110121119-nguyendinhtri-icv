export interface IExperience {
    id?: string;
    documentId?: string;
    title?: string;
    position?: string;
    currentlyWorking?: boolean;
    description?: string;
    startDate?: string; 
    endDate?: string;   
}